#include "replacement.hpp"

int
fifo(FrameList* active_frames, FrameList** frame_to_remove_ptr)
{
  return -1;
}


int
lru(FrameList* active_frames, FrameList** frame_to_remove_ptr)
{
  return -1;
}


int clock_lru(FrameList* active_frames, FrameList** frame_to_remove_ptr)
{
  return -1;
}

